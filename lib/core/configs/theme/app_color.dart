import 'package:flutter/material.dart';

class AppColor {
  // Primary Color - Use for the app’s main accents: navigation bar, headers, and important icons.
  static const Color primaryColor = Color(0xff4FC3F7);

  // Secondary Color - Use for buttons, action highlights, and badges.
  static const Color secondaryColor = Color(0xff81C784);

  // Scaffold Background Color - Use as the scaffold color, where most content is displayed.
  static const Color scaffoldBGColor = Color(0xffF5F5DC);

  // Dark Mode Background Color.
  static const Color darkBackgroundColor = Color(0xff0D0C0C);

  // Primary Text Color - Use for main text.
  static const Color textColor = Color(0xff212121);

  // White Text Color - Use for light mode or dark backgrounds.
  static const Color textWhiteColor = Color(0xffF9F6EE);
}
